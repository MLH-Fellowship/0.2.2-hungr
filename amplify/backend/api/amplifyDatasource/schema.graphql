type Restaurant @model {
  id: ID!
  name: String!
  rating: Int
  photo: String!
}

type Room @model {
  id: ID!
  code: String
  restaurants: [Restaurant]
  users: [User]
  size: Int
  current: Restaurant
  lat: Int
  long: Int
  cuisine: String
}

type User @model {
  id: ID!
}
